# Copie este arquivo para .env
#   cp .env.example .env      (ou copiar/colar no Windows)

# URL do backend. Opcional — se não definir, o padrão já é este.
NEXT_PUBLIC_API_URL="http://localhost:3001"
